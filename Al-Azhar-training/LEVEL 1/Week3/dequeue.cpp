#include <iostream>
#include <deque>
using namespace std;

int main(){

deque <int> d;

// same as queue but the Diffrenece is that cpp STL library provides some extra functionalites here, which allows for better control over the normal queue

d.push_back(1); // puts one in the rear
d.push_front(3); // put 3 in the front of the queue
d.pop_back(); // removes last element
d.pop_front(); // removes the front element

// now the queue is empty lol














  return 0;
}
